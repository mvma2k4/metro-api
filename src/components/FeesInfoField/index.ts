import FeesInfoFieldService from './service';
import { HttpError } from '../../config/error';
import { IFeesInfoFieldModel } from './model';
import { NextFunction, Request, Response } from 'express';

/**
 * @export
 * @param {Request} req
 * @param {Response} res
 * @param {NextFunction} next
 * @returns {Promise < void >}
 */
export async function findAll(req: Request, res: Response, next: NextFunction): Promise < void > {
    try {
        const counters: IFeesInfoFieldModel[] = await FeesInfoFieldService.findAll();

        res.status(200).json(counters);
    } catch (error) {
        next(new HttpError(error.message.status, error.message));
    }
}

/**
 * @export
 * @param {Request} req
 * @param {Response} res
 * @param {NextFunction} next
 * @returns {Promise < void >}
 */
export async function findOne(req: Request, res: Response, next: NextFunction): Promise < void > {
    try {
        const FeesInfoField: IFeesInfoFieldModel = await FeesInfoFieldService.findOne(req.params.id);

        res.status(200).json(FeesInfoField);
    } catch (error) {
        next(new HttpError(error.message.status, error.message));
    }
}

/**
 * @export
 * @param {Request} req
 * @param {Response} res
 * @param {NextFunction} next
 * @returns {Promise < void >}
 */
export async function create(req: Request, res: Response, next: NextFunction): Promise < void > {
    try {
        const FeesInfoField: IFeesInfoFieldModel = await FeesInfoFieldService.insert(req.body);

        res.status(201).json(FeesInfoField);
    } catch (error) {
        next(new HttpError(error.message.status, error.message));
    }
}

/**
 * @export
 * @param {Request} req
 * @param {Response} res
 * @param {NextFunction} next
 * @returns {Promise < void >}
 */
export async function update(req: Request, res: Response, next: NextFunction): Promise < void > {
    try {
        const FeesInfoField: IFeesInfoFieldModel = await FeesInfoFieldService.updateOne(req.body.uuid, req.body);

        res.status(201).json(FeesInfoField);
    } catch (error) {
        next(new HttpError(error.message.status, error.message));
    }
}

/**
 * @export
 * @param {Request} req
 * @param {Response} res
 * @param {NextFunction} next
 * @returns {Promise < void >}
 */
export async function remove(req: Request, res: Response, next: NextFunction): Promise < void > {
    try {
        await FeesInfoFieldService.remove(req.params.id);

        res.status(200).json(null);
    } catch (error) {
        next(new HttpError(error.message.status, error.message));
    }
}
